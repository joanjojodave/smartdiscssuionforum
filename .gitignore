# ── Environment / secrets ────────────────────────────────────
.env
.env.backup
.env.production
.env.testing
/auth.json

# ── Editors / IDEs (unanchored: catches nested project folders too,
#    e.g. desktop-client/.idea if opened as its own IntelliJ project) ──
.idea/
.vscode/
.nova/
.zed/
.cursor/
.claude/
.codex/
.phpactor.json

# ── OS cruft ──────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Laravel app ───────────────────────────────────────────────
/vendor
/node_modules
/public/build
/public/hot
/public/fonts-manifest.dev.json
/public/storage
/storage/*.key
/storage/pail
/database/database.sqlite
.phpunit.cache
.phpunit.result.cache
_ide_helper.php
Homestead.json
Homestead.yaml

# ── Java desktop client (Maven) ──────────────────────────────
desktop-client/target/
desktop-client/dependency-reduced-pom.xml

# ── Logs (any location, either app) ──────────────────────────
*.log
